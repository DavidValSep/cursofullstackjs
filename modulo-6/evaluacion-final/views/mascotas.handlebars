<h1>Registro Nacional de Mascotas</h1>

<div class="catalog-list">
    {{#each duenos}}
    <li class="owner-card">
        <div class="owner-info">
            <h3>Dueño: {{this.nombre_dueno}}</h3>
            <p><strong>RUT:</strong> {{this.rut_dueno}}</p>
        </div>
        
        <div class="pet-list-container">
            <strong>Mascotas:</strong>
            <ul>
                {{#each this.mascotas}}
                <li>🐾 {{this.nombre}} ({{this.especie}})</li>
                {{/each}}
            </ul>
        </div>

        <div class="actions">
            <!-- Botón de ejemplo para ver detalle del dueño -->
            <a href="/detalle/{{this.rut_dueno}}" class="btn-secondary">Gestionar Dueño</a>
        </div>
    </li>
    {{/each}}
</div>